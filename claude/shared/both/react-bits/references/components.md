# React Bits 元件完整目錄(139 個)

格式:**元件名** (`官網頁面路徑`) — 描述 [deps: npm 相依套件]。
安裝:`pnpm dlx shadcn@latest add @react-bits/<元件名>-<TS|JS>-<TW|CSS>`;
原始碼:`https://reactbits.dev/r/<元件名>-TS-TW.json`。
目錄產自官方 shadcn registry(2026-07-20)。

## 文字動畫(Text Animations)

- **ASCIIText** (`/text-animations/ascii-text`) — Renders text with an animated ASCII background for a retro feel. [deps: three]
- **BlurText** (`/text-animations/blur-text`) — Text starts blurred then crisply resolves for a soft-focus reveal effect. [deps: motion]
- **CircularText** (`/text-animations/circular-text`) — Layouts characters around a circle with optional rotation animation. [deps: motion]
- **CountUp** (`/text-animations/count-up`) — Animated number counter supporting formatting and decimals. [deps: motion]
- **CurvedLoop** (`/text-animations/curved-loop`) — Flowing looping text path along a customizable curve with drag interaction. [deps: 無]
- **DecryptedText** (`/text-animations/decrypted-text`) — Hacker-style decryption cycling random glyphs until resolving to real text. [deps: motion]
- **FallingText** (`/text-animations/falling-text`) — Characters fall with gravity + bounce creating a playful entrance. [deps: matter-js]
- **FuzzyText** (`/text-animations/fuzzy-text`) — Vibrating fuzzy text with controllable hover intensity. [deps: 無]
- **GlitchText** (`/text-animations/glitch-text`) — RGB split and distortion glitch effect with jitter effects. [deps: 無]
- **GradientText** (`/text-animations/gradient-text`) — Animated gradient sweep across live text with speed and color control. [deps: motion]
- **RotatingText** (`/text-animations/rotating-text`) — Cycles through multiple phrases with 3D rotate / flip transitions. [deps: motion]
- **ScrambledText** (`/text-animations/scrambled-text`) — Detects cursor position and applies a distortion effect to text. [deps: gsap]
- **ScrollFloat** (`/text-animations/scroll-float`) — Text gently floats / parallax shifts on scroll. [deps: gsap]
- **ScrollReveal** (`/text-animations/scroll-reveal`) — Text gently unblurs and reveals on scroll. [deps: gsap]
- **ScrollVelocity** (`/text-animations/scroll-velocity`) — Text marquee animatio - speed and distortion scale with user's scroll velocity. [deps: motion]
- **ShinyText** (`/text-animations/shiny-text`) — Metallic sheen sweeps across text producing a reflective highlight. [deps: motion]
- **Shuffle** (`/text-animations/shuffle`) — Animated text reveal where characters shuffle before settling. [deps: @gsap/react, gsap]
- **SplitText** (`/text-animations/split-text`) — Splits text into characters / words for staggered entrance animation. [deps: @gsap/react, gsap]
- **TextCursor** (`/text-animations/text-cursor`) — Make any text element follow your cursor, leaving a trail of copies behind it. [deps: motion]
- **TextPressure** (`/text-animations/text-pressure`) — Characters scale / warp interactively based on pointer pressure zone. [deps: 無]
- **TextType** (`/text-animations/text-type`) — Typewriter effect with blinking cursor and adjustable typing cadence. [deps: gsap]
- **TrueFocus** (`/text-animations/true-focus`) — Applies dynamic blur / clarity based over a series of words in order. [deps: motion]
- **VariableProximity** (`/text-animations/variable-proximity`) — Letter styling changes continuously with pointer distance mapping. [deps: motion]

## 動畫效果(Animations)

- **AnimatedContent** (`/animations/animated-content`) — Wrapper that animates any children on scroll or mount with configurable direction, distance, duration, easing and disappear options. [deps: gsap]
- **Antigravity** (`/animations/antigravity`) — 3D antigravity particle field that repels from the cursor with smooth motion. [deps: @react-three/fiber, three]
- **BlobCursor** (`/animations/blob-cursor`) — Organic blob cursor that smoothly follows the pointer with inertia and elastic morphing. [deps: gsap]
- **ClickSpark** (`/animations/click-spark`) — Creates particle spark bursts at click position. [deps: 無]
- **Crosshair** (`/animations/crosshair`) — Custom crosshair cursor with tracking, and link hover effects. [deps: gsap]
- **Cubes** (`/animations/cubes`) — 3D rotating cube cluster. Supports auto-rotation or hover interaction. [deps: gsap]
- **CursorGrid** (`/animations/cursor-grid`) — Canvas grid whose cells light up around the cursor with configurable radius, falloff and click pulses. [deps: 無]
- **ElectricBorder** (`/animations/electric-border`) — Jittery electric energy border with animated arcs, glow and adjustable intensity. [deps: 無]
- **FadeContent** (`/animations/fade-content`) — Simple directional fade / slide entrance / exit wrapper with threshold-based activation. [deps: gsap]
- **GhostCursor** (`/animations/ghost-cursor`) — Semi-transparent ghost cursor that smoothly follows the real cursor with a trailing effect. [deps: three]
- **GlareHover** (`/animations/glare-hover`) — Adds a realistic moving glare highlight on hover over any element. [deps: 無]
- **GradualBlur** (`/animations/gradual-blur`) — Progressively un-blurs content based on scroll or trigger creating a cinematic reveal. [deps: 無]
- **ImageTrail** (`/animations/image-trail`) — Cursor-based image trail with several built-in variants. [deps: gsap]
- **LaserFlow** (`/animations/laser-flow`) — Dynamic laser light that flows onto a surface, customizable effect. [deps: three]
- **LogoLoop** (`/animations/logo-loop`) — Continuously looping marquee of brand or tech logos with seamless repeat and hover pause. [deps: 無]
- **MagicRings** (`/animations/magic-rings`) — Interactive magic rings effect with customizable parameters. [deps: three]
- **Magnet** (`/animations/magnet`) — Elements magnetically ease toward the cursor then settle back with spring physics. [deps: 無]
- **MagnetLines** (`/animations/magnet-lines`) — Animated field lines bend toward the cursor. [deps: 無]
- **MetaBalls** (`/animations/meta-balls`) — Liquid metaball blobs that merge and separate with smooth implicit surface animation. [deps: ogl]
- **MetallicPaint** (`/animations/metallic-paint`) — Liquid metallic paint shader which can be applied to SVG elements. [deps: 無]
- **Noise** (`/animations/noise`) — Animated film grain / noise overlay adding subtle texture and motion. [deps: 無]
- **OrbitImages** (`/animations/orbit-images`) — SVG Path customizable orbiting images effect [deps: motion]
- **PixelTrail** (`/animations/pixel-trail`) — Pixelated cursor trail emitting fading squares with retro digital feel. [deps: @react-three/drei, @react-three/fiber, three]
- **PixelTransition** (`/animations/pixel-transition`) — Pixel dissolve transition for content reveal on hover. [deps: gsap]
- **Ribbons** (`/animations/ribbons`) — Flowing responsive ribbons/cursor trail driven by physics and pointer motion. [deps: ogl]
- **ShapeBlur** (`/animations/shape-blur`) — Morphing blurred geometric shape. The effect occurs on hover. [deps: three]
- **SplashCursor** (`/animations/splash-cursor`) — Liquid splash burst at cursor with curling ripples and waves. [deps: 無]
- **StarBorder** (`/animations/star-border`) — Animated star / sparkle border orbiting content with twinkle pulses. [deps: 無]
- **StickerPeel** (`/animations/sticker-peel`) — Sticker corner lift + peel interaction using 3D transform and shadow depth. [deps: gsap]
- **Strands** (`/animations/strands`) — Glowing ribbon-like strands that ripple and weave across a transparent canvas. [deps: ogl]
- **TargetCursor** (`/animations/target-cursor`) — A cursor follow animation with 4 corners that lock onto targets. [deps: gsap]

## 元件(Components)

- **AnimatedList** (`/components/animated-list`) — List items enter with staggered motion variants for polished reveals. [deps: motion]
- **BorderGlow** (`/components/border-glow`) — Glowing mesh-gradient border that follows cursor direction and intensifies near edges. [deps: 無]
- **BounceCards** (`/components/bounce-cards`) — Cards bounce that bounce in on mount. [deps: gsap]
- **BubbleMenu** (`/components/bubble-menu`) — Floating circular expanding menu with staggered item reveal. [deps: gsap]
- **CardNav** (`/components/card-nav`) — Expandable navigation bar with card panels revealing nested links. [deps: gsap, react-icons]
- **CardSwap** (`/components/card-swap`) — Cards animate position swapping with smooth layout transitions. [deps: gsap]
- **Carousel** (`/components/carousel`) — Responsive carousel with touch gestures, looping and transitions. [deps: motion, react-icons]
- **ChromaGrid** (`/components/chroma-grid`) — A responsive grid of grayscale tiles. Hovering the grid reaveals their colors. [deps: gsap]
- **CircularGallery** (`/components/circular-gallery`) — Circular orbit gallery rotating images. [deps: ogl]
- **Counter** (`/components/counter`) — Flexible animated counter supporting increments + easing. [deps: motion]
- **CurvedInput** (`/components/curved-input`) — Arc-bent input bar with text, caret and submit button all following the curve. [deps: 無]
- **DecayCard** (`/components/decay-card`) — Hover parallax effect that disintegrates the content of a card. [deps: gsap]
- **Dock** (`/components/dock`) — macOS style magnifying dock with proximity scaling of icons. [deps: motion]
- **DomeGallery** (`/components/dome-gallery`) — Immersive 3D dome gallery projecting images on a hemispheric surface. [deps: @use-gesture/react]
- **ElasticSlider** (`/components/elastic-slider`) — Slider handle stretches elastically then snaps with spring physics. [deps: motion]
- **FlowingMenu** (`/components/flowing-menu`) — Liquid flowing active indicator glides between menu items. [deps: gsap]
- **FluidGlass** (`/components/fluid-glass`) — Glassmorphism container with animated liquid distortion refraction. [deps: @react-three/drei, @react-three/fiber, maath, three]
- **FlyingPosters** (`/components/flying-posters`) — 3D posters rotate on scroll infinitely. [deps: ogl]
- **Folder** (`/components/folder`) — Interactive folder opens to reveal nested content smooth motion. [deps: 無]
- **GlassIcons** (`/components/glass-icons`) — Icon set styled with frosted glass blur. [deps: 無]
- **GlassSurface** (`/components/glass-surface`) — Advanced Apple-style glass surface with real-time distortion + lighting. [deps: 無]
- **GooeyNav** (`/components/gooey-nav`) — Navigation indicator morphs with gooey blob transitions between items. [deps: 無]
- **InfiniteMenu** (`/components/infinite-menu`) — Horizontally looping menu effect that scrolls endlessly with seamless wrap. [deps: gl-matrix]
- **Lanyard** (`/components/lanyard`) — Swinging 3D lanyard / badge card with realistic inertial motion. [deps: 無]
- **LineSidebar** (`/components/line-sidebar`) — Static list navigation with a cursor-proximity effect that shifts and highlights nearby items. [deps: 無]
- **MagicBento** (`/components/magic-bento`) — Interactive bento grid tiles expand + animate with various options. [deps: gsap]
- **Masonry** (`/components/masonry`) — Responsive masonry layout with animated reflow + gaps optimization. [deps: gsap]
- **ModelViewer** (`/components/model-viewer`) — Three.js model viewer with orbit controls and lighting presets. [deps: @react-three/drei, @react-three/fiber, three]
- **OptionWheel** (`/components/option-wheel`) — Curved option picker that spins via scroll, drag, or arrow keys, fading and tilting items away from the selection. [deps: 無]
- **PillNav** (`/components/pill-nav`) — Minimal pill nav with sliding active highlight + smooth easing. [deps: gsap, react-router-dom]
- **PixelCard** (`/components/pixel-card`) — Card content revealed through pixel expansion transition. [deps: 無]
- **ProfileCard** (`/components/profile-card`) — Animated profile card glare with 3D hover effect. [deps: 無]
- **ReflectiveCard** (`/components/reflective-card`) — Card with dynamic webcam reflection and glare effects that respond to cursor movement. [deps: lucide-react]
- **ScrollStack** (`/components/scroll-stack`) — Overlapping card stack reveals on scroll with depth layering. [deps: lenis]
- **SpecularButton** (`/components/specular-button`) — Glass button with a shader-driven specular rim light that sweeps around the edge and follows the cursor. [deps: ogl]
- **SpotlightCard** (`/components/spotlight-card`) — Dynamic spotlight follows cursor casting gradient illumination. [deps: 無]
- **Stack** (`/components/stack`) — Layered stack with swipe animations, autoplay and smooth transitions. [deps: motion]
- **StaggeredMenu** (`/components/staggered-menu`) — Menu with staggered item animations and smooth transitions on open/close. [deps: gsap]
- **Stepper** (`/components/stepper`) — Animated multi-step progress indicator with active state transitions. [deps: motion]
- **TiltedCard** (`/components/tilted-card`) — 3D perspective tilt card reacting to pointer. [deps: motion]

## 背景(Backgrounds)

- **Aurora** (`/backgrounds/aurora`) — Flowing aurora gradient background. [deps: ogl]
- **Balatro** (`/backgrounds/balatro`) — The balatro shader, fully customizalbe and interactive. [deps: ogl]
- **Ballpit** (`/backgrounds/ballpit`) — Physics ball pit simulation with bouncing colorful spheres. [deps: gsap, three]
- **Beams** (`/backgrounds/beams`) — Crossing animated ribbons with customizable properties. [deps: @react-three/drei, @react-three/fiber, three]
- **ColorBends** (`/backgrounds/color-bends`) — Vibrant color bends with smooth flowing animation. [deps: three]
- **DarkVeil** (`/backgrounds/dark-veil`) — Subtle dark background with a smooth animation and postprocessing. [deps: ogl]
- **Dither** (`/backgrounds/dither`) — Retro dithered noise shader background. [deps: @react-three/fiber, @react-three/postprocessing, postprocessing, three]
- **DotField** (`/backgrounds/dot-field`) — Interactive dot grid with cursor bulge, glow, sparkle, and wave effects. [deps: 無]
- **DotGrid** (`/backgrounds/dot-grid`) — Animated dot grid with cursor interactions. [deps: gsap]
- **EvilEye** (`/backgrounds/evil-eye`) — Procedural evil eye shader with animated iris, slit pupil, and fiery outer glow. [deps: ogl]
- **FaultyTerminal** (`/backgrounds/faulty-terminal`) — Terminal CRT scanline squares effect with flicker + noise. [deps: ogl]
- **Ferrofluid** (`/backgrounds/ferrofluid`) — A churning magnetic fluid traced by glowing contour lines, with a cursor magnet. [deps: ogl]
- **FloatingLines** (`/backgrounds/floating-lines`) — 3D floating lines that react to cursor movement. [deps: three]
- **Galaxy** (`/backgrounds/galaxy`) — Parallax realistic starfield with pointer interactions. [deps: ogl]
- **GradientBlinds** (`/backgrounds/gradient-blinds`) — Layered gradient blinds with spotlight and noise distortion. [deps: ogl]
- **Grainient** (`/backgrounds/grainient`) — Grainy gradient swirls with soft wave distortion. [deps: ogl]
- **GridDistortion** (`/backgrounds/grid-distortion`) — Warped grid mesh distorts smoothly reacting to cursor. [deps: three]
- **GridMotion** (`/backgrounds/grid-motion`) — Perspective moving grid lines based on cusror position. [deps: gsap]
- **GridScan** (`/backgrounds/grid-scan`) — Animated grid room 3D scan effect and cool interactions. [deps: face-api.js, postprocessing, three]
- **Hyperspeed** (`/backgrounds/hyperspeed`) — Animated lines continuously moving to simulate hyperspace travel on click hold. [deps: postprocessing, three]
- **Iridescence** (`/backgrounds/iridescence`) — Slick iridescent shader with shifting waves. [deps: ogl]
- **LetterGlitch** (`/backgrounds/letter-glitch`) — Matrix style letter animation. [deps: 無]
- **LightPillar** (`/backgrounds/light-pillar`) — Vertical pillar of light with glow effects. [deps: three]
- **LightRays** (`/backgrounds/light-rays`) — Volumetric light rays/beams with customizable direction. [deps: ogl]
- **Lightfall** (`/backgrounds/lightfall`) — Colorful light streaks raining down a glowing tunnel with a cursor light. [deps: ogl]
- **Lightning** (`/backgrounds/lightning`) — Procedural lightning bolts with branching and glow flicker. [deps: 無]
- **LineWaves** (`/backgrounds/line-waves`) — Animated line wave pattern with colorful warped distortion. [deps: ogl]
- **LiquidChrome** (`/backgrounds/liquid-chrome`) — Liquid metallic chrome shader with flowing reflective surface. [deps: ogl]
- **LiquidEther** (`/backgrounds/liquid-ether`) — Interactive liquid shader with flowing distortion and customizable colors. [deps: three]
- **Orb** (`/backgrounds/orb`) — Floating energy orb with customizable hover effect. [deps: ogl]
- **Particles** (`/backgrounds/particles`) — Configurable particle system. [deps: ogl]
- **PixelBlast** (`/backgrounds/pixel-blast`) — Exploding pixel particle bursts with optional liquid postprocessing. [deps: postprocessing, three]
- **PixelSnow** (`/backgrounds/pixel-snow`) — Falling pixelated snow effect with customizable density and speed. [deps: three]
- **Plasma** (`/backgrounds/plasma`) — Organic plasma gradients swirl + morph with smooth turbulence. [deps: ogl]
- **PlasmaWave** (`/backgrounds/plasma-wave`) — Raymarched plasma waves with dual-wave interference and OGL. [deps: ogl]
- **Prism** (`/backgrounds/prism`) — Rotating prism with configurable intensity, size, and colors. [deps: ogl]
- **PrismaticBurst** (`/backgrounds/prismatic-burst`) — Burst of light rays with controllable color, distortion, amount. [deps: ogl]
- **Radar** (`/backgrounds/radar`) — Radar sweep effect with concentric rings, radial spokes, and a rotating beam. [deps: ogl]
- **RippleGrid** (`/backgrounds/ripple-grid`) — A grid that continuously animates with a ripple effect. [deps: ogl]
- **ShapeGrid** (`/backgrounds/shape-grid`) — Animated grid with shape variants (square, hexagon, circle, triangle) + direction customization. [deps: 無]
- **SideRays** (`/backgrounds/side-rays`) — Animated light rays emanating from the side with customizable colors and speed. [deps: ogl]
- **Silk** (`/backgrounds/silk`) — Smooth waves background with soft lighting. [deps: @react-three/fiber, three]
- **SoftAurora** (`/backgrounds/soft-aurora`) — Soft aurora borealis shader with 3D Perlin noise and cosine gradient palettes. [deps: ogl]
- **Threads** (`/backgrounds/threads`) — Animated pattern of lines forming a fabric-like motion. [deps: ogl]
- **Waves** (`/backgrounds/waves`) — Layered lines that form smooth wave patterns with animation. [deps: 無]