{
  "allowNonWorkspaceAccess": true,
  "model": "Gemini 3.5 Flash (High)",
  "permissions": {
    "allow": [
      "command(ls)",
      "command(find)",
      "command(xargs)",
      "command(git status)",
      "command(which)",
      "command(codex)",
      "command(uv)",
      "command(pnpm check)",
      "command(git diff)",
      "command(openspec)",
      "command(pnpm run)",
      "command(poetry)",
      "command(git checkout)",
      "command(docker compose)",
      "command(tail)",
      "command(cd)",
      "command(git branch)",
      "command(git log)",
      "command(head)",
      "command(pnpm tsc)",
      "command(pytest)"
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "5h limit 7day",
    "enabled": false
  },
  "toolPermission": "always-proceed",
  "trustedWorkspaces": [
    "/home/human/jtai",
    "/home/human/arcreel360",
    "/home/human/openVman",
    "/home/human/ai-config"
  ]
}
